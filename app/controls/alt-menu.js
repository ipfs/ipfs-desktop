// var init = require('./../init')
// var ipc = require('ipc')

exports = module.exports = altMenu

function altMenu (event, bounds) {
  if (!event.altKey) {
    return
  }

  // TODO open a menubar like window, but simpler options (settings + quit)
}
